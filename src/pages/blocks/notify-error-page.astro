---
import SidebarLayout from '../../layouts/SidebarLayout.astro';
---
<SidebarLayout title="Error Page">
  <main>
<ui-stack gap="6">
    <div>
      <h1>Error Pages</h1>
      <p style="color: var(--neutral-ink-muted); font-size: 0.875rem;">
        Full-page error patterns for common HTTP errors and maintenance windows.
        Each variant provides clear messaging and an actionable recovery path.
      </p>
    </div>

    <!-- -- 404 Not Found -- -->

    <ui-stack gap="4">
    <h2>404 — Not Found</h2>
    <div class="error-page">
      <div class="error-code" aria-hidden="true">404</div>
      <h3>Page not found</h3>
      <p>The page you're looking for doesn't exist or has been moved. Check the URL or head back to the homepage.</p>
      <ui-button variant="primary" intent="accent">
        <ui-icon name="house" slot="leading"></ui-icon>
        <span slot="label">Go Home</span>
      </ui-button>
    </div>
    </ui-stack>

    <!-- -- 500 Server Error -- -->

    <ui-stack gap="4">
    <h2>500 — Server Error</h2>
    <div class="error-page">
      <div class="error-code" aria-hidden="true">500</div>
      <h3>Server error</h3>
      <p>Something went wrong on our end. Our team has been notified and is working on a fix. Please try again in a few moments.</p>
      <ui-stack direction="row" gap="3">
        <ui-button variant="primary" intent="accent" id="retry-btn">
          <span slot="label">Try Again</span>
        </ui-button>
        <ui-button>
          <ui-icon name="house" slot="leading"></ui-icon>
          <span slot="label">Go Home</span>
        </ui-button>
      </ui-stack>
    </div>
    </ui-stack>

    <!-- -- Maintenance -- -->

    <ui-stack gap="4">
    <h2>Maintenance</h2>
    <div class="error-page">
      <ui-icon name="gear" size="xl" aria-hidden="true" style="color: var(--neutral-ink-muted); margin-bottom: 1rem;"></ui-icon>
      <h3>We'll be back soon</h3>
      <p>We're performing scheduled maintenance to improve your experience. This usually takes less than 30 minutes. Thank you for your patience.</p>
      <ui-button>
        <ui-icon name="info" slot="leading"></ui-icon>
        <span slot="label">Check Status</span>
      </ui-button>
    </div>
    </ui-stack>
    </ui-stack>
  </main>
  <style is:global>
    .error-page {
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      text-align: center;
      padding: 4rem 2rem;
      background: var(--neutral-body);
      border: 1px solid var(--neutral-border-muted);
      border-radius: 0.75rem;
      min-height: 20rem;
    }

    .error-code {
      font-size: 4.5rem;
      font-weight: 800;
      line-height: 1;
      color: var(--neutral-ink-muted);
      margin-bottom: 0.75rem;
      letter-spacing: -0.02em;
    }

    .error-page h3 {
      font-size: 1.25rem;
      font-weight: 700;
      margin: 0 0 0.5rem;
    }

    .error-page p {
      font-size: 0.875rem;
      color: var(--neutral-ink-muted);
      margin: 0 0 1.5rem;
      max-width: 28rem;
      line-height: 1.5;
    }

</style>

</SidebarLayout>

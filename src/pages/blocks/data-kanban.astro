---
import SidebarLayout from '../../layouts/SidebarLayout.astro';
---
<SidebarLayout title="Kanban Board">
  <header>
    <div slot="title">
      <div slot="content">
        <h1>Kanban Board</h1>
      </div>
    </div>
    <div slot="description">

      Project task board with draggable cards across status columns.
      Uses the <code>draggable</code> trait in <code>slot</code> mode — drag a card to see the
      insertion indicator showing exactly where it will land.

    </div>
  </header>
  <section>
<n-stack gap="6">

    <n-stack direction="row" gap="3" align="center" wrap>
      <n-input size="sm" placeholder="Filter tasks..." id="kanban-filter">
        <n-icon name="magnifying-glass" slot="leading"></n-icon>
      </n-input>
      <n-button size="sm" intent="accent">
        <n-icon name="plus" slot="leading"></n-icon>
        <span>New Task</span>
      </n-button>
    </n-stack>

    <n-controller traits="draggable"
      data-trait-draggable-selector=".kanban-card"
      data-trait-draggable-zone-selector=".kanban-column-body"
      data-trait-draggable-axis="vertical"
      data-trait-draggable-mode="slot">

      <div class="board" id="kanban-board">
        <!-- ── To Do ── -->
        <div class="kanban-column">
          <div class="kanban-column-header">
            <span class="kanban-column-title">
              <span class="kanban-dot todo"></span>
              To Do
            </span>
            <n-badge>4</n-badge>
          </div>
          <div class="kanban-column-body" data-column="todo">
            <div class="kanban-card">
              <div class="kanban-card-header">
                <n-badge intent="info" size="sm">Feature</n-badge>
                <span class="kanban-id">KAN-12</span>
              </div>
              <p class="kanban-card-title">Add keyboard shortcuts to command palette</p>
              <div class="kanban-card-footer">
                <n-avatar name="Kim Granlund" size="xs" intent="accent">KG</n-avatar>
                <span class="kanban-meta"><n-icon name="chat-circle" size="sm" intent="success"></n-icon> 3</span>
              </div>
            </div>
            <div class="kanban-card">
              <div class="kanban-card-header">
                <n-badge intent="warning" size="sm">Bug</n-badge>
                <span class="kanban-id">KAN-15</span>
              </div>
              <p class="kanban-card-title">Fix tooltip positioning on scroll</p>
              <div class="kanban-card-footer">
                <n-avatar name="Alex Morgan" size="xs" intent="info">AM</n-avatar>
                <span class="kanban-meta"><n-icon name="chat-circle" size="sm" intent="success"></n-icon> 1</span>
              </div>
            </div>
            <div class="kanban-card">
              <div class="kanban-card-header">
                <n-badge size="sm">Task</n-badge>
                <span class="kanban-id">KAN-18</span>
              </div>
              <p class="kanban-card-title">Write unit tests for validation trait</p>
              <div class="kanban-card-footer">
                <n-avatar name="Sara Chen" size="xs" intent="success">SC</n-avatar>
              </div>
            </div>
            <div class="kanban-card">
              <div class="kanban-card-header">
                <n-badge intent="info" size="sm">Feature</n-badge>
                <span class="kanban-id">KAN-21</span>
              </div>
              <p class="kanban-card-title">Design dark mode color tokens</p>
              <div class="kanban-card-footer">
                <n-avatar name="Kim Granlund" size="xs" intent="accent">KG</n-avatar>
                <span class="kanban-meta"><n-icon name="chat-circle" size="sm" intent="success"></n-icon> 5</span>
              </div>
            </div>
          </div>
        </div>

        <!-- ── In Progress ── -->
        <div class="kanban-column">
          <div class="kanban-column-header">
            <span class="kanban-column-title">
              <span class="kanban-dot progress"></span>
              In Progress
            </span>
            <n-badge>3</n-badge>
          </div>
          <div class="kanban-column-body" data-column="progress">
            <div class="kanban-card">
              <div class="kanban-card-header">
                <n-badge intent="info" size="sm">Feature</n-badge>
                <span class="kanban-id">KAN-09</span>
              </div>
              <p class="kanban-card-title">Implement accordion animation transitions</p>
              <div class="kanban-card-footer">
                <n-avatar name="James Rivera" size="xs" intent="warning">JR</n-avatar>
                <span class="kanban-meta"><n-icon name="chat-circle" size="sm" intent="success"></n-icon> 7</span>
              </div>
            </div>
            <div class="kanban-card">
              <div class="kanban-card-header">
                <n-badge intent="warning" size="sm">Bug</n-badge>
                <span class="kanban-id">KAN-11</span>
              </div>
              <p class="kanban-card-title">Table overflow on narrow viewports</p>
              <div class="kanban-card-footer">
                <n-avatar name="Alex Morgan" size="xs" intent="info">AM</n-avatar>
                <span class="kanban-meta"><n-icon name="chat-circle" size="sm" intent="success"></n-icon> 2</span>
              </div>
            </div>
            <div class="kanban-card">
              <div class="kanban-card-header">
                <n-badge size="sm">Task</n-badge>
                <span class="kanban-id">KAN-14</span>
              </div>
              <p class="kanban-card-title">Update component documentation with new API</p>
              <div class="kanban-card-footer">
                <n-avatar name="Sara Chen" size="xs" intent="success">SC</n-avatar>
              </div>
            </div>
          </div>
        </div>

        <!-- ── Done ── -->
        <div class="kanban-column">
          <div class="kanban-column-header">
            <span class="kanban-column-title">
              <span class="kanban-dot done"></span>
              Done
            </span>
            <n-badge>3</n-badge>
          </div>
          <div class="kanban-column-body" data-column="done">
            <div class="kanban-card">
              <div class="kanban-card-header">
                <n-badge intent="info" size="sm">Feature</n-badge>
                <span class="kanban-id">KAN-03</span>
              </div>
              <p class="kanban-card-title">Add segmented control component</p>
              <div class="kanban-card-footer">
                <n-avatar name="Kim Granlund" size="xs" intent="accent">KG</n-avatar>
                <span class="kanban-meta"><n-icon name="check" size="sm" intent="success"></n-icon></span>
              </div>
            </div>
            <div class="kanban-card">
              <div class="kanban-card-header">
                <n-badge intent="warning" size="sm">Bug</n-badge>
                <span class="kanban-id">KAN-06</span>
              </div>
              <p class="kanban-card-title">Fix dialog autofocus on showModal()</p>
              <div class="kanban-card-footer">
                <n-avatar name="James Rivera" size="xs" intent="warning">JR</n-avatar>
                <span class="kanban-meta"><n-icon name="check" size="sm" intent="success"></n-icon></span>
              </div>
            </div>
            <div class="kanban-card">
              <div class="kanban-card-header">
                <n-badge size="sm">Task</n-badge>
                <span class="kanban-id">KAN-07</span>
              </div>
              <p class="kanban-card-title">Migrate CSS tokens to OKLCH color space</p>
              <div class="kanban-card-footer">
                <n-avatar name="Alex Morgan" size="xs" intent="info">AM</n-avatar>
                <span class="kanban-meta"><n-icon name="check" size="sm" intent="success"></n-icon></span>
              </div>
            </div>
          </div>
        </div>
      </div>
    </n-controller>
    </n-stack>
  </section>

  <script>
    function setup() {
      requestAnimationFrame(() => {
        const board = document.getElementById('kanban-board');
        if (!board || board.hasAttribute('data-wired')) return;
        board.setAttribute('data-wired', '');
        const filterInput = document.querySelector('#kanban-filter');

        filterInput?.addEventListener('input', () => {
          const query = (filterInput.value || '').toLowerCase();
          for (const card of board.querySelectorAll('.kanban-card')) {
            const title = card.querySelector('.kanban-card-title')?.textContent?.toLowerCase() || '';
            card.style.display = title.includes(query) ? '' : 'none';
          }
          updateCounts();
        });

        function updateCounts() {
          board.querySelectorAll('.kanban-column').forEach((col) => {
            const cards = col.querySelectorAll('.kanban-card');
            let count = 0;
            for (const c of cards) if (c.style.display !== 'none') count++;
            const badge = col.querySelector('.kanban-column-header n-badge');
            if (badge) badge.textContent = String(count);
          });
        }

        board.addEventListener('native:drop', (e) => {
          const { item, insertBefore, targetZone } = e.detail;
          if (!item) return;
          if (insertBefore) {
            insertBefore.before(item);
          } else if (targetZone) {
            targetZone.appendChild(item);
          }
          updateCounts();
        });
      });
    }

    setup();
    document.addEventListener('astro:page-load', setup);
  </script>
  <style is:global>
    .board {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 1rem;
      min-height: 24rem;
    }

    @media (max-width: 48rem) {
      .board {
        grid-template-columns: 1fr;
      }
    }

    .kanban-column {
      display: flex;
      flex-direction: column;
      gap: 0.75rem;
      background: var(--n-body-neutral);
      border: 1px solid var(--n-border-muted-neutral);
      border-radius: 0.5rem;
      padding: 0.75rem;
    }

    .kanban-column-header {
      display: flex;
      align-items: center;
      justify-content: space-between;
      padding: 0.25rem 0.25rem 0.5rem;
    }

    .kanban-column-title {
      display: flex;
      align-items: center;
      gap: 0.5rem;
      font-size: 0.8125rem;
      font-weight: 700;
    }

    .kanban-dot {
      width: 0.5rem;
      height: 0.5rem;
      border-radius: 50%;
    }

    .kanban-dot.todo { background: var(--n-color-neutral-400); }
    .kanban-dot.progress { background: var(--n-color-accent-500); }
    .kanban-dot.done { background: var(--n-color-success-500); }

    .kanban-column-body {
      display: flex;
      flex-direction: column;
      gap: 0.625rem;
      min-height: 4rem;
      padding: 0.25rem;
      border-radius: 0.375rem;
      transition: outline-color 150ms ease, background 150ms ease;
      outline: 2px dashed transparent;
      outline-offset: -2px;
    }

    .kanban-column-body[drag-zone-active] {
      outline-color: var(--n-color-accent-400);
      background: oklch(from var(--n-color-accent-500) l c h / 0.04);
    }

    /* Slot mode gap indicator */
    .kanban-column-body > .drag-placeholder {
      height: 3px;
      background: var(--n-color-accent-500);
      border-radius: 2px;
      margin: 0.125rem 0;
    }

    .kanban-card {
      display: flex;
      flex-direction: column;
      gap: 0.625rem;
      padding: 0.75rem;
      background: var(--n-panel-neutral);
      border: 1px solid var(--n-border-muted-neutral);
      border-radius: 0.375rem;
      cursor: grab;
      user-select: none;
      transition: box-shadow 150ms ease, border-color 150ms ease, opacity 150ms ease;
    }

    .kanban-card:hover {
      border-color: var(--n-border-neutral);
      box-shadow: 0 2px 8px oklch(0 0 0 / 0.06);
    }

    .kanban-card[dragging] {
      opacity: 0.3;
      cursor: grabbing;
    }

    .kanban-card-header {
      display: flex;
      align-items: center;
      justify-content: space-between;
    }

    .kanban-id {
      font-size: 0.6875rem;
      color: var(--n-ink-muted-neutral);
      font-family: ui-monospace, monospace;
    }

    .kanban-card-title {
      font-size: 0.8125rem;
      font-weight: 500;
      line-height: 1.4;
      margin: 0;
    }

    .kanban-card-footer {
      display: flex;
      align-items: center;
      gap: 0.5rem;
      margin-top: 0.25rem;
    }

    .kanban-meta {
      display: flex;
      align-items: center;
      gap: 0.25rem;
      font-size: 0.6875rem;
      color: var(--n-ink-muted-neutral);
      margin-left: auto;
    }

    #kanban-filter {
      max-width: 16rem;
    }

</style>

</SidebarLayout>

---
import SidebarLayout from '../../layouts/SidebarLayout.astro';
---
<SidebarLayout title="ui-calendar">
  <main>
<h1>&lt;ui-calendar&gt;</h1>
    <p style="color: var(--neutral-ink-muted); font-size: 0.875rem; margin-bottom: 1.5rem;">
      Date selection with day, month, and year views. Supports single selection, date range, min/max constraints, and keyboard navigation.
    </p>

    <!-- ── Single Select ── -->

    <h2>Single Select</h2>
    <div class="layout-section">
      <ui-calendar id="single"></ui-calendar>
      <div id="single-output" class="output">Select a date      <pre class="layout-code"><code>&lt;ui-calendar&gt;&lt;/ui-calendar&gt;</code><ui-button class="copy-btn" size="sm" variant="ghost" aria-label="Copy"><ui-icon name="copy"></ui-icon></ui-button></pre>
</div>
    </div>

    <!-- ── With Initial Value ── -->

    <h2>Initial Value</h2>
    <div class="layout-section">
      <ui-calendar value="2026-02-14"></ui-calendar>
      <pre class="layout-code"><code>&lt;ui-calendar value="2026-02-14"&gt;&lt;/ui-calendar&gt;</code><ui-button class="copy-btn" size="sm" variant="ghost" aria-label="Copy"><ui-icon name="copy"></ui-icon></ui-button></pre>
    </div>

    <!-- ── Min/Max Constraints ── -->

    <h2>Min/Max Constraints</h2>
    <div class="layout-section">
      <ui-calendar min="2026-02-10" max="2026-02-25"></ui-calendar>
      <p style="font-size: 0.75rem; color: var(--neutral-ink-muted); margin-top: 0.5rem;">
        Only dates between Feb 10–25 are selectable.
      </p>
      <pre class="layout-code"><code>&lt;ui-calendar min="2026-02-10" max="2026-02-25"&gt;&lt;/ui-calendar&gt;</code><ui-button class="copy-btn" size="sm" variant="ghost" aria-label="Copy"><ui-icon name="copy"></ui-icon></ui-button></pre>
    </div>

    <!-- ── Range Selection ── -->

    <h2>Range Selection</h2>
    <div class="layout-section">
      <ui-calendar id="range-cal" range></ui-calendar>
      <div id="range-output" class="output">Click a start date, hover to preview range, click to commit. Click again to clear.      <pre class="layout-code"><code>&lt;ui-calendar range&gt;&lt;/ui-calendar&gt;</code><ui-button class="copy-btn" size="sm" variant="ghost" aria-label="Copy"><ui-icon name="copy"></ui-icon></ui-button></pre>
</div>
    </div>

    <!-- ── Sizes ── -->

    <h2>Sizes</h2>
    <div class="layout-section">
      <div style="display: flex; flex-wrap: wrap; gap: 1.5rem; align-items: start;">
        <div>
          <p style="font-size: 0.75rem; font-weight: 600; text-transform: uppercase; letter-spacing: 0.05em; color: var(--neutral-ink-muted); margin: 0 0 0.5rem;">sm</p>
          <ui-calendar size="sm"></ui-calendar>
        </div>
        <div>
          <p style="font-size: 0.75rem; font-weight: 600; text-transform: uppercase; letter-spacing: 0.05em; color: var(--neutral-ink-muted); margin: 0 0 0.5rem;">md (default)</p>
          <ui-calendar></ui-calendar>
        </div>
        <div>
          <p style="font-size: 0.75rem; font-weight: 600; text-transform: uppercase; letter-spacing: 0.05em; color: var(--neutral-ink-muted); margin: 0 0 0.5rem;">lg</p>
          <ui-calendar size="lg"></ui-calendar>
        </div>
      </div>
      <pre class="layout-code"><code>&lt;ui-calendar size="sm"&gt;&lt;/ui-calendar&gt;
&lt;ui-calendar&gt;&lt;/ui-calendar&gt;
&lt;ui-calendar size="lg"&gt;&lt;/ui-calendar&gt;</code><ui-button class="copy-btn" size="sm" variant="ghost" aria-label="Copy"><ui-icon name="copy"></ui-icon></ui-button></pre>
    </div>

    <!-- ── Disabled ── -->

    <h2>Disabled</h2>
    <div class="layout-section">
      <ui-calendar value="2026-02-20" disabled></ui-calendar>
      <pre class="layout-code"><code>&lt;ui-calendar value="2026-02-20" disabled&gt;&lt;/ui-calendar&gt;</code><ui-button class="copy-btn" size="sm" variant="ghost" aria-label="Copy"><ui-icon name="copy"></ui-icon></ui-button></pre>
    </div>

    <!-- ── Debug States (force-*) ── -->

    <h2>Debug States (<code>force-*</code>)</h2>
    <div class="layout-section">
      <p style="font-size: 0.75rem; color: var(--neutral-ink-muted); margin: 0 0 1rem;">
        Attribute-driven pseudo-state overrides for DevTools debugging and visual testing. Applied to the host element, targets internal cells and buttons.
      </p>
      <div style="display: flex; gap: 1.5rem; flex-wrap: wrap;">
        <div>
          <div style="font-size: 0.625rem; font-weight: 600; text-transform: uppercase; letter-spacing: 0.04em; color: var(--neutral-ink-muted); margin-bottom: 0.5rem;">force-hover</div>
          <ui-calendar value="2026-02-20" force-hover></ui-calendar>
        </div>
        <div>
          <div style="font-size: 0.625rem; font-weight: 600; text-transform: uppercase; letter-spacing: 0.04em; color: var(--neutral-ink-muted); margin-bottom: 0.5rem;">force-focus-visible</div>
          <ui-calendar value="2026-02-20" force-focus-visible></ui-calendar>
        </div>
      </div>
    </div>
  </main>

  <script>
// Single select output
      const single = document.getElementById('single');
      const singleOutput = document.getElementById('single-output');
      single.addEventListener('ui-change', (e) => {
        singleOutput.textContent = `Selected: ${e.detail.value}`;
      });

      // Range output
      const rangeCal = document.getElementById('range-cal');
      const rangeOutput = document.getElementById('range-output');
      rangeCal.addEventListener('ui-range-select', (e) => {
        rangeOutput.textContent = `Range: ${e.detail.start} → ${e.detail.end}`;
      });
// ── Copy buttons ──

      for (const btn of document.querySelectorAll('.copy-btn')) {
        btn.addEventListener('click', async () => {
          const code = btn.closest('.layout-code')?.querySelector('code');
          if (!code) return;
          await navigator.clipboard.writeText(code.textContent);
          const icon = btn.querySelector('ui-icon');
          if (icon) {
            icon.setAttribute('name', 'check');
            setTimeout(() => { icon.setAttribute('name', 'copy'); }, 1500);
          }
        });
      }
  </script>
  <style is:global>
    .output {
      font-family: monospace;
      font-size: 0.75rem;
      color: var(--neutral-ink-muted);
      margin-top: 0.75rem;
      padding: 0.5rem;
      background: var(--neutral-panel);
      border-radius: 0.25rem;
      min-height: 1.5rem;
    }

    /* ── Code Blocks ── */
</style>

</SidebarLayout>

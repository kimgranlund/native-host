---
import SidebarLayout from '../../layouts/SidebarLayout.astro';
---
<SidebarLayout title="Footer">
  <header>
    <div slot="title">
      <div slot="content">
        <h1>Footer</h1>
      </div>
    </div>
    <div slot="description">
      <span class="header-tag">&lt;n-footer&gt;</span> CSS-only sub-container for bottom-anchored actions. Flex row, end-aligned by default. Works inside n-container, n-container, n-drawer, n-dialog, or any flex column parent.
          </div>
  </header>

  <section>

    <!-- ── Basic ── -->

    <h2>Basic</h2>
    <n-container data-kind="panel" bordered>
      <div class="footer-card-wrap">
        <n-container>
          <n-header>
            <span>Panel Title</span>
          </n-header>
          <n-body>
            <span>Content above the footer. The footer sits at the bottom with action buttons end-aligned by default.</span>
          </n-body>
          <n-footer>
            <n-button variant="ghost"><span>Cancel</span></n-button>
            <n-button variant="primary" intent="accent"><span>Save</span></n-button>
          </n-footer>
        </n-container>
      </div>
    <pre class="code-block"><code>&lt;n-container&gt;
  &lt;n-header&gt;
    &lt;span slot="label"&gt;Panel Title&lt;/span&gt;
  &lt;/n-header&gt;
  &lt;n-body&gt;
    &lt;p&gt;Content above the footer.&lt;/p&gt;
  &lt;/n-body&gt;
  &lt;n-footer&gt;
    &lt;n-button variant="ghost"&gt;Cancel&lt;/n-button&gt;
    &lt;n-button variant="primary" intent="accent"&gt;Save&lt;/n-button&gt;
  &lt;/n-footer&gt;
&lt;/n-container&gt;</code><n-button size="sm" variant="ghost" aria-label="Copy"><n-icon name="copy"></n-icon></n-button></pre>
    </n-container>

    <!-- ── Justify Variants ── -->

    <h2>Justify Variants</h2>
    <n-container data-kind="panel" bordered>
      <n-stack gap="4">
        <div class="footer-card-wrap">
          <n-container>
            <n-header><span>End (default)</span></n-header>
            <n-body><span>Actions aligned to the right.</span></n-body>
            <n-footer>
              <n-button variant="ghost"><span>Cancel</span></n-button>
              <n-button variant="primary" intent="accent"><span>Confirm</span></n-button>
            </n-footer>
          </n-container>
        </div>
        <div class="footer-card-wrap">
          <n-container>
            <n-header><span>Spread</span></n-header>
            <n-body><span>Cancel left, Save right.</span></n-body>
            <n-footer justify="spread">
              <n-button variant="ghost"><span>Cancel</span></n-button>
              <n-button variant="primary" intent="accent"><span>Save</span></n-button>
            </n-footer>
          </n-container>
        </div>
        <div class="footer-card-wrap">
          <n-container>
            <n-header><span>Start</span></n-header>
            <n-body><span>Actions aligned left.</span></n-body>
            <n-footer justify="start">
              <n-button variant="ghost"><span>Back</span></n-button>
              <n-button variant="primary" intent="accent"><span>Next</span></n-button>
            </n-footer>
          </n-container>
        </div>
        <div class="footer-card-wrap">
          <n-container>
            <n-header><span>Center</span></n-header>
            <n-body><span>Centered actions.</span></n-body>
            <n-footer justify="center">
              <n-button variant="primary" intent="accent"><span>Got it</span></n-button>
            </n-footer>
          </n-container>
        </div>
      </n-stack>
    <pre class="code-block"><code>&lt;n-footer&gt;...&lt;/n-footer&gt;                   &lt;!-- end (default) --&gt;
&lt;n-footer justify="spread"&gt;...&lt;/n-footer&gt;  &lt;!-- space-between --&gt;
&lt;n-footer justify="start"&gt;...&lt;/n-footer&gt;   &lt;!-- left-aligned --&gt;
&lt;n-footer justify="center"&gt;...&lt;/n-footer&gt;  &lt;!-- centered --&gt;</code><n-button size="sm" variant="ghost" aria-label="Copy"><n-icon name="copy"></n-icon></n-button></pre>
    </n-container>

    <!-- ── Dividers ── -->

    <h2>Dividers</h2>
    <n-container data-kind="panel" bordered>
      <div class="footer-card-wrap">
        <n-container>
          <n-header><span>With Divider</span></n-header>
          <n-body>
            <span>The <code>dividers</code> attribute adds a top border line to visually separate the footer from the body.</span>
          </n-body>
          <n-footer dividers>
            <n-button variant="ghost"><span>Cancel</span></n-button>
            <n-button variant="primary" intent="accent"><span>Apply</span></n-button>
          </n-footer>
        </n-container>
      </div>
    <pre class="code-block"><code>&lt;n-footer dividers&gt;
  &lt;n-button variant="ghost"&gt;Cancel&lt;/n-button&gt;
  &lt;n-button variant="primary" intent="accent"&gt;Apply&lt;/n-button&gt;
&lt;/n-footer&gt;</code><n-button size="sm" variant="ghost" aria-label="Copy"><n-icon name="copy"></n-icon></n-button></pre>
    </n-container>

    <!-- ── Sticky ── -->

    <h2>Sticky</h2>
    <n-container data-kind="panel" bordered>
      <div class="footer-card-wrap footer-card-wrap--fixed-height">
        <n-container>
          <n-header><span>Scrollable with Sticky Footer</span></n-header>
          <n-body>
            <span>When content scrolls, the sticky footer stays anchored at the bottom of the scrollable container.</span>
            <span>Lorem ipsum dolor sit amet, consectetur adipiscing elit. Sed do eiusmod tempor incididunt ut labore et dolore magna aliqua.</span>
            <span>Ut enim ad minim veniam, quis nostrud exercitation ullamco laboris nisi ut aliquip ex ea commodo consequat.</span>
            <span>Duis aute irure dolor in reprehenderit in voluptate velit esse cillum dolore eu fugiat nulla pariatur.</span>
            <span>Excepteur sint occaecat cupidatat non proident, sunt in culpa qui officia deserunt mollit anim id est laborum.</span>
          </n-body>
          <n-footer sticky dividers>
            <n-button variant="primary" intent="accent"><span>Done</span></n-button>
          </n-footer>
        </n-container>
      </div>
    <pre class="code-block"><code>&lt;n-container style="height: 16rem;"&gt;
  &lt;n-header&gt;&lt;span slot="label"&gt;Scrollable&lt;/span&gt;&lt;/n-header&gt;
  &lt;n-body&gt;
    &lt;p&gt;Long scrollable content...&lt;/p&gt;
  &lt;/n-body&gt;
  &lt;n-footer sticky dividers&gt;
    &lt;n-button variant="primary" intent="accent"&gt;Done&lt;/n-button&gt;
  &lt;/n-footer&gt;
&lt;/n-container&gt;</code><n-button size="sm" variant="ghost" aria-label="Copy"><n-icon name="copy"></n-icon></n-button></pre>
    </n-container>

    <!-- ── With Card ── -->

    <h2>With Card</h2>
    <n-container data-kind="panel" bordered>
      <n-stack direction="row" wrap align="start">
        <div class="footer-card-wrap footer-card-wrap--narrow">
          <n-container>
            <div slot="media" class="footer-media footer-media--accent"></div>
            <n-body>
              <h3 class="footer-card-title">Product Name</h3>
              <span class="text-sm text-muted">A short description of the product.</span>
            </n-body>
            <n-footer>
              <n-button variant="primary" intent="accent" size="sm"><span>Buy Now</span></n-button>
            </n-footer>
          </n-container>
        </div>
        <div class="footer-card-wrap footer-card-wrap--narrow">
          <n-container>
            <div slot="media" class="footer-media footer-media--success"></div>
            <n-body>
              <h3 class="footer-card-title">Another Item</h3>
              <span class="text-sm text-muted">Another brief description.</span>
            </n-body>
            <n-footer justify="spread">
              <span class="text-sm text-muted">$29.99</span>
              <n-button variant="primary" intent="accent" size="sm"><span>Add</span></n-button>
            </n-footer>
          </n-container>
        </div>
      </n-stack>
    <pre class="code-block"><code>&lt;n-container&gt;
  &lt;div slot="media"&gt;...&lt;/div&gt;
  &lt;n-body&gt;
    &lt;h3&gt;Product Name&lt;/h3&gt;
    &lt;span class=&quot;text-sm text-muted&quot;&gt;Description.&lt;/span&gt;
  &lt;/n-body&gt;
  &lt;n-footer&gt;
    &lt;n-button variant="primary" intent="accent" size="sm"&gt;Buy Now&lt;/n-button&gt;
  &lt;/n-footer&gt;
&lt;/n-container&gt;</code><n-button size="sm" variant="ghost" aria-label="Copy"><n-icon name="copy"></n-icon></n-button></pre>
    </n-container>

    <!-- ── With Dialog ── -->

    <h2>With Dialog</h2>
    <n-container data-kind="panel" bordered>
      <n-button id="open-footer-dialog"><span>Open Dialog</span></n-button>
      <n-dialog id="footer-dialog-demo">
        <n-header>
          <span>Confirm Action</span>
          <n-button variant="ghost" id="close-footer-dialog"><n-icon name="x"></n-icon></n-button>
        </n-header>
        <n-body>
          <span>Are you sure you want to proceed? This action cannot be undone.</span>
        </n-body>
        <n-footer justify="spread">
          <n-button variant="ghost" id="cancel-footer-dialog"><span>Cancel</span></n-button>
          <n-button variant="primary" intent="danger"><span>Delete</span></n-button>
        </n-footer>
      </n-dialog>
    <pre class="code-block"><code>&lt;n-dialog&gt;
  &lt;n-header&gt;
    &lt;span slot="label"&gt;Confirm Action&lt;/span&gt;
    &lt;n-button slot="trailing" variant="ghost"&gt;&lt;n-icon name="x"&gt;&lt;/n-icon&gt;&lt;/n-button&gt;
  &lt;/n-header&gt;
  &lt;n-body&gt;
    &lt;p&gt;Are you sure you want to proceed?&lt;/p&gt;
  &lt;/n-body&gt;
  &lt;n-footer justify="spread"&gt;
    &lt;n-button variant="ghost"&gt;Cancel&lt;/n-button&gt;
    &lt;n-button variant="primary" intent="danger"&gt;Delete&lt;/n-button&gt;
  &lt;/n-footer&gt;
&lt;/n-dialog&gt;</code><n-button size="sm" variant="ghost" aria-label="Copy"><n-icon name="copy"></n-icon></n-button></pre>
    </n-container>

    <!-- ── Padding ── -->

    <h2>Padding</h2>
    <n-container data-kind="panel" bordered>
      <n-stack gap="4">
        <div class="footer-card-wrap">
          <n-container>
            <n-header><span>None</span></n-header>
            <n-body><span>No padding on the footer.</span></n-body>
            <n-footer padding="none">
              <n-button variant="primary" intent="accent"><span>OK</span></n-button>
            </n-footer>
          </n-container>
        </div>
        <div class="footer-card-wrap">
          <n-container>
            <n-header><span>Tight (default)</span></n-header>
            <n-body><span>Default compact padding.</span></n-body>
            <n-footer padding="tight">
              <n-button variant="primary" intent="accent"><span>OK</span></n-button>
            </n-footer>
          </n-container>
        </div>
        <div class="footer-card-wrap">
          <n-container>
            <n-header><span>Regular</span></n-header>
            <n-body><span>More breathing room.</span></n-body>
            <n-footer padding="regular">
              <n-button variant="primary" intent="accent"><span>OK</span></n-button>
            </n-footer>
          </n-container>
        </div>
        <div class="footer-card-wrap">
          <n-container>
            <n-header><span>Relaxed</span></n-header>
            <n-body><span>Spacious footer padding.</span></n-body>
            <n-footer padding="relaxed">
              <n-button variant="primary" intent="accent"><span>OK</span></n-button>
            </n-footer>
          </n-container>
        </div>
      </n-stack>
    <pre class="code-block"><code>&lt;n-footer padding="none"&gt;...&lt;/n-footer&gt;
&lt;n-footer padding="tight"&gt;...&lt;/n-footer&gt;    &lt;!-- default --&gt;
&lt;n-footer padding="regular"&gt;...&lt;/n-footer&gt;
&lt;n-footer padding="relaxed"&gt;...&lt;/n-footer&gt;</code><n-button size="sm" variant="ghost" aria-label="Copy"><n-icon name="copy"></n-icon></n-button></pre>
    </n-container>

    <!-- ── API ── -->

    <h2>API</h2>
    <n-container data-kind="panel" bordered>
      <table class="api-table">
        <thead>
          <tr>
            <th>Attribute</th>
            <th>Values</th>
            <th>Description</th>
          </tr>
        </thead>
        <tbody>
          <tr>
            <td><code>justify</code></td>
            <td><code>start</code> | <code>center</code> | <code>spread</code></td>
            <td>Horizontal alignment. Default is <code>end</code> (flex-end).</td>
          </tr>
          <tr>
            <td><code>padding</code></td>
            <td><code>none</code> | <code>tight</code> | <code>regular</code> | <code>relaxed</code></td>
            <td>Override inner padding. Default is tight.</td>
          </tr>
          <tr>
            <td><code>dividers</code></td>
            <td>boolean</td>
            <td>Adds a top border line via <code>::before</code> pseudo-element.</td>
          </tr>
          <tr>
            <td><code>sticky</code></td>
            <td>boolean</td>
            <td>Sticks footer to the bottom of a scrollable container (<code>position: sticky</code>).</td>
          </tr>
        </tbody>
      </table>
    </n-container>
  </section>

  <script>
    const dialog = document.getElementById('footer-dialog-demo');
    document.getElementById('open-footer-dialog')?.addEventListener('native:press', () => dialog?.showModal());
    document.getElementById('close-footer-dialog')?.addEventListener('native:press', () => dialog?.close());
    document.getElementById('cancel-footer-dialog')?.addEventListener('native:press', () => dialog?.close());
  </script>
</SidebarLayout>

<style is:global>
  .footer-card-wrap {
    max-width: 28rem;
  }
  .footer-card-wrap--narrow {
    max-width: 18rem;
  }
  .footer-card-wrap--fixed-height n-container {
    height: 16rem;
  }
  .footer-media {
    height: 8rem;
  }
  .footer-media--accent {
    background: linear-gradient(135deg, var(--n-surface-accent), var(--n-surface-info));
  }
  .footer-media--success {
    background: linear-gradient(135deg, var(--n-surface-success), var(--n-surface-warning));
  }
  .footer-card-title {
    margin: 0 0 0.25rem;
    text-transform: none;
    letter-spacing: 0;
    color: inherit;
  }
  .api-table {
    width: 100%;
    border-collapse: collapse;
    font-size: var(--n-font-size);
    line-height: var(--n-line-height);
  }
  .api-table th,
  .api-table td {
    text-align: left;
    padding: calc(var(--n-space) * 2) calc(var(--n-space) * 3);
    border-bottom: 1px solid var(--n-border-muted);
  }
  .api-table th {
    font-weight: 600;
    color: var(--n-ink-strong);
  }
  .api-table code {
    font-family: var(--n-font-mono);
    font-size: 0.875em;
    background: var(--n-control);
    padding: 0.125em 0.375em;
    border-radius: var(--n-radius);
  }
</style>

---
import SidebarLayout from '../../layouts/SidebarLayout.astro';
---
<SidebarLayout title="Divider">
  <header>
    <div slot="title">
      <div slot="content">
        <h1>Divider</h1>
      </div>
    </div>
    <div slot="description">
      <span class="header-tag">&lt;n-divider&gt;</span> Horizontal or vertical rule between content sections. CSS-only container.
          </div>
  </header>

  <section>

    <!-- ── Basic ── -->

    <h2>Horizontal (default)</h2>
    <n-container data-kind="panel" bordered>
      <p>Content above the divider.</p>
      <n-divider></n-divider>
      <p>Content below the divider.</p>
    <pre class="code-block"><code>&lt;p&gt;Content above the divider.&lt;/p&gt;
&lt;n-divider&gt;&lt;/n-divider&gt;
&lt;p&gt;Content below the divider.&lt;/p&gt;</code><n-button size="sm" variant="ghost" aria-label="Copy"><n-icon name="copy"></n-icon></n-button></pre>
    </n-container>

    <!-- ── With Label ── -->

    <h2>With Label</h2>
    <n-container data-kind="panel" bordered>
      <p>Sign in with your account.</p>
      <n-divider>or</n-divider>
      <p>Continue as guest.</p>
    </n-container>

    <n-container data-kind="panel" bordered>
      <n-divider>or continue with</n-divider>
    <pre class="code-block"><code>&lt;n-divider&gt;or&lt;/n-divider&gt;

&lt;n-divider&gt;or continue with&lt;/n-divider&gt;</code><n-button size="sm" variant="ghost" aria-label="Copy"><n-icon name="copy"></n-icon></n-button></pre>
    </n-container>

    <!-- ── Vertical ── -->

    <h2>Vertical</h2>
    <n-container data-kind="panel" bordered>
      <div class="vertical-container"><n-stack direction="row" align="center" gap="4">
        <span>Left</span>
        <n-divider orientation="vertical"></n-divider>
        <span>Right</span>
      </n-stack></div>
    </n-container>

    <n-container data-kind="panel" bordered>
      <div class="vertical-container"><n-stack direction="row" align="center" gap="4">
        <span>One</span>
        <n-divider orientation="vertical"></n-divider>
        <span>Two</span>
        <n-divider orientation="vertical"></n-divider>
        <span>Three</span>
      </n-stack></div>
      <pre class="code-block"><code>&lt;div style="display: flex; align-items: center; gap: 1rem; height: 4rem;"&gt;
  &lt;span&gt;Left&lt;/span&gt;
  &lt;n-divider orientation="vertical"&gt;&lt;/n-divider&gt;
  &lt;span&gt;Right&lt;/span&gt;
&lt;/div&gt;</code><n-button size="sm" variant="ghost" aria-label="Copy"><n-icon name="copy"></n-icon></n-button></pre>
    </n-container>

    <!-- ── Vertical With Label ── -->

    <h2>Vertical With Label</h2>
    <n-container data-kind="panel" bordered>
      <div class="vertical-container vertical-container--tall"><n-stack direction="row" align="center" gap="4">
        <span>Left</span>
        <n-divider orientation="vertical">or</n-divider>
        <span>Right</span>
      </n-stack></div>
      <pre class="code-block"><code>&lt;n-divider orientation="vertical"&gt;or&lt;/n-divider&gt;</code><n-button size="sm" variant="ghost" aria-label="Copy"><n-icon name="copy"></n-icon></n-button></pre>
    </n-container>

    <!-- ── Sizes ── -->

    <h2>Sizes (inherit from parent)</h2>
    <n-container data-kind="panel" bordered>
      <h3>size="xs"</h3>
      <div size="xs">
        <n-divider>xs label</n-divider>
      </div>
      <h3>size="sm"</h3>
      <div size="sm">
        <n-divider>sm label</n-divider>
      </div>
      <h3>size="md"</h3>
      <div size="md">
        <n-divider>md label</n-divider>
      </div>
      <h3>size="lg"</h3>
      <div size="lg">
        <n-divider>lg label</n-divider>
      </div>
      <h3>size="xl"</h3>
      <div size="xl">
        <n-divider>xl label</n-divider>
      </div>
      <pre class="code-block"><code>&lt;div size="sm"&gt;
  &lt;n-divider&gt;sm label&lt;/n-divider&gt;
&lt;/div&gt;</code><n-button size="sm" variant="ghost" aria-label="Copy"><n-icon name="copy"></n-icon></n-button></pre>
    </n-container>

    <!-- ── Composition ── -->

    <h2>Composition (with buttons)</h2>
    <n-container data-kind="panel" bordered>
      <div class="divider-full-btn"><n-button variant="primary" intent="accent"><span>Sign In</span></n-button></div>
      <n-divider>or continue with</n-divider>
      <n-stack direction="row">
        <div class="divider-flex-btn"><n-button><n-icon name="google-logo" slot="leading"></n-icon><span>Google</span></n-button></div>
        <div class="divider-flex-btn"><n-button><n-icon name="github-logo" slot="leading"></n-icon><span>GitHub</span></n-button></div>
      </n-stack>
      <pre class="code-block"><code>&lt;n-button variant="primary" intent="accent" style="width: 100%;"&gt;Sign In&lt;/n-button&gt;
&lt;n-divider&gt;or continue with&lt;/n-divider&gt;
&lt;div style="display: flex; gap: 0.5rem;"&gt;
  &lt;n-button style="flex: 1;"&gt;Google&lt;/n-button&gt;
  &lt;n-button style="flex: 1;"&gt;GitHub&lt;/n-button&gt;
&lt;/div&gt;</code><n-button size="sm" variant="ghost" aria-label="Copy"><n-icon name="copy"></n-icon></n-button></pre>
    </n-container>
  </section>

  <style is:global>
    .vertical-container {
      height: 4rem;
    }

    .vertical-container--tall {
      height: 6rem;
    }

    .divider-full-btn n-button { width: 100%; }
    .divider-flex-btn { flex: 1; }

    /* ── Code Blocks ── */
</style>

</SidebarLayout>
